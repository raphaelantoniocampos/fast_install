[project]
name = "windeploy"
version = "0.1.5"
description = "Automated Windows Deployment Tool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "inquirerpy>=0.3.4",
    "rich>=13.9.4",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.12.0",
    "ruff>=0.9.0",
]
