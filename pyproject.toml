[project]
name = "fast-install"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "inquirerpy>=0.3.4",
    "rich>=13.9.4",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.12.0",
    "ruff>=0.9.0",
]
